apiVersion: apps/v1
kind: Deployment
metadata:
  name: allibdae-admin
  labels:
    app: allibdae-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: allibdae-admin
  template:
    metadata:
      labels:
        app: allibdae-admin
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
      - name: allibdae-admin
        image: ghcr.io/mohamedsalem66/allibdae-admin:0.1-SNAPSHOT
        ports:
        - containerPort: 3000
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 30

---

apiVersion: v1
kind: Service
metadata:
  name: allibdae-admin
spec:
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: allibdae-admin
